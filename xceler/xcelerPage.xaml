<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:xceler" 
    x:Class="xceler.xcelerPage" BackgroundColor="#2f4259"><ScrollView>
    <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <Label Text="Welcome to Xcelerator!" VerticalOptions="Center" HorizontalOptions="Center" FontSize="30 " TextColor="White" />
    </StackLayout>
     <StackLayout Padding="40" Spacing="10">
                 <Label  HorizontalOptions="CenterAndExpand" 
                              Text="Xcelerator" 
                              TextColor="White" 
                              FontSize="40" />
                <Label TextColor="White"  Text="{Binding UserDetails.FirstName}" HorizontalOptions="FillAndExpand"/>
                <Label TextColor="White" Text="{Binding UserDetails.Lastname}" HorizontalOptions="FillAndExpand"/>
                <Label TextColor="White"  Text="{Binding UserDetails.Email}"  HorizontalOptions="FillAndExpand"/>
                <Label TextColor="White"  Text="{Binding UserDetails.Mobile}" HorizontalOptions="FillAndExpand"/>

              
             </StackLayout></ScrollView>
	
</ContentPage>
